apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  name: {{ .Release.Name }}-metadata-server
spec:
  ports:
    - name: grpc
      port: {{ .Values.grpc.port }}
      protocol: TCP
      targetPort: 8080
  selector:
    run: {{ .Release.Name }}-metadata-server
status:
  loadBalancer: {}
