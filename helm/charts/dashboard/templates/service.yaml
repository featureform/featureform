apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dashboard
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-dashboard
  ports:
    - port: {{ .Values.global.port }}
      targetPort: 3000
      protocol: TCP
      nodePort: 31001
