apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-prometheus-deployment
  labels:
    app: sandbox-prometheus-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sandbox-prometheus-server
  template:
    metadata:
      labels:
        app: sandbox-prometheus-server
    spec:
      containers:
        - name: sandbox-prometheus
          image: {{ .Values.image.repository }}
          args:
            - "--config.file=/etc/prometheus/prometheus.yml"
            - "--storage.tsdb.path=/prometheus/"
            - "--web.route-prefix=/prometheus"
            - "--web.external-url=http://my.featureform.com/prometheus"
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: sandbox-prometheus-config-volume
              mountPath: /etc/prometheus/
            - name: sandbox-prometheus-storage-volume
              mountPath: /prometheus/
      volumes:
        - name: sandbox-prometheus-config-volume
          configMap:
            defaultMode: 420
            name: sandbox-prometheus-server-conf

        - name: sandbox-prometheus-storage-volume
          emptyDir: {}
