apiVersion: v1
kind: Service
metadata:
  name: {{ include "dashboard-metadata.fullname" . }}
  labels:
    {{- include "dashboard-metadata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dashboard-metadata.selectorLabels" . | nindent 4 }}
