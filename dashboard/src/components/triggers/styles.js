import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(() => ({
  inputRow: {
    paddingBottom: 10,
  },
  triggerBox: {
    height: 750,
    width: 700,
  },
}));

export { useStyles };
