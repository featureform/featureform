apiVersion: v1
kind: Service
metadata:
  name: sandbox-dashboard
spec:
  type: NodePort
  selector:
    app: sandbox-dashboard
  ports:
    - port: {{ .Values.dashboard.port }}
      targetPort: 3000
      protocol: TCP
      nodePort: 31001
