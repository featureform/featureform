apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    run: {{ .Release.Name }}-feature-server
  name: {{ .Release.Name }}-feature-server
spec:
  replicas: 1
  selector:
    matchLabels:
      run: {{ .Release.Name }}-feature-server
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: {{ .Release.Name }}-feature-server
    spec:
      containers:
        - image: {{ .Values.image.repository }}
          name: {{ .Release.Name }}-feature-server
          ports:
            - containerPort: 8080
            - containerPort: 2112
          env:
            - name: SERVING_PORT
              value: "localhost:8080"
          resources: {}
status: {}
