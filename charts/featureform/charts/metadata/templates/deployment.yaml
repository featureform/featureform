apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    run: featureform-metadata-server
  name: featureform-metadata-server
spec:
  replicas: 1
  selector:
    matchLabels:
      run: featureform-metadata-server
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: featureform-metadata-server
    spec:
      containers:
        - image: {{ .Values.image.repository }}
          name: featureform-metadata-server
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          resources: {}
          env:
            - name: TYPESENSE_PORT
              value: "80"
            - name: TYPESENSE_HOST
              value: featureform-typesense
            - name: TYPESENSE_APIKEY
              value: xyz
            - name: ETCD_HOST
              value: featureform-etcd
            - name: ETCD_PORT
              value: "2379"
status: {}
